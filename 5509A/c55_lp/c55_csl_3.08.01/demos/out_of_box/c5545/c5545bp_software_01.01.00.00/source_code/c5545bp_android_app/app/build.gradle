apply plugin: 'com.android.application'

android {
    compileSdkVersion 23
    buildToolsVersion "22.0.0"
    defaultConfig {
        applicationId "android.projects.com.c5545ble"
        minSdkVersion 19
        targetSdkVersion 23
        versionCode 4
        versionName "1.4"
    }
    signingConfigs {
        release {
            storeFile file('/home/mistral/Android/AndroidProjects/Projects/Keystore/c5545_keystore.jks')
            storePassword "boosterpackti"
            keyAlias "c5545"
            keyPassword "boosterpackti"
        }
    }
    buildTypes {

        release {
            signingConfig signingConfigs.release
        }
    }
    productFlavors {
    }
}

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    compile 'com.android.support:appcompat-v7:23.0.1'
}
